# Terraform EC2 Lab

## What I Practiced
- Set up Ubuntu VM and installed Terraform
- Created a Terraform project with proper file structure
- Configured AWS provider and created a simple EC2 instance
- Applied infrastructure with `terraform apply` and checked outputs
- Destroyed resources with `terraform destroy` to clean up
